//When const is used to declare a variable, the valu can't re re-assigned. But if it's an object like the example we have below, the value can always be changed

const person = {
    name: 'Mere Daniel',
    age: 20
}

person.name = 'Mere Ugochukwu'

console.log(person)
